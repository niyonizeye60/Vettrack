// TODO: no notification sound asset exists in public/ yet. Once one is supplied
// (e.g. public/sounds/notification.mp3), play it here via `new Audio(...).play()`.
export function playChatNotificationSound() {
  // no-op until an audio asset is supplied
}
